{
  "name": "ApiTokenCache",
  "type": "object",
  "properties": {
    "provider": {
      "type": "string",
      "title": "Provider"
    },
    "access_token": {
      "type": "string",
      "title": "Access Token"
    },
    "expires_at": {
      "type": "string",
      "title": "Expires At",
      "format": "date-time"
    },
    "retry_after": {
      "type": "string",
      "title": "Retry After (rate-limit hint from provider)"
    }
  },
  "required": [
    "provider",
    "expires_at"
  ]
}